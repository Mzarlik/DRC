Options -Indexes

# Bloquear acceso a cualquier archivo .env
<FilesMatch "^\.env">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# Bloquear archivos de configuración de dependencias, git y planes de implementación
<FilesMatch "^(composer\.json|composer\.lock|\.gitignore|\.gitattributes|\.agents|CONTEXTO\.md|TESTING_SECURITY\.md|implementation_plan\.md)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# Bloquear navegación directa en el directorio core
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^core/.*$ - [F,L]
</IfModule>

# Bloquear acceso directo a exportaciones, reportes, logs, caché y archivos SQL
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^public/(exports|reports)/ - [F,L]
    RewriteRule ^(logs|cache)/ - [F,L]
    RewriteRule \.(sql|bak|old|sql\.gz)$ - [F,L]
</IfModule>
